// Enum

enum Color {
  blue, red, yellow
}

void main() {
  // Print Color
  
	// Print Color Using forEach
  
	// Call Color Type Color as Color.blue
  
  print(color);
  
	// Change Color as Red
  
  print(color);
}